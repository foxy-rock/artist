const swiper = new Swiper('.swiper-jury', {
  // Optional parameters
  loop: true,
  centeredSlides: true,
  slidesPerView: 3.5,

  // If we need pagination
  pagination: {
    el: '.swiper-pagination',
    clickable: true,
  },

});